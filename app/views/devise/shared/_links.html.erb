<%- if controller_name != 'sessions' %>
  <%= link_to "تسجيل الدخول", new_session_path(resource_name), class: "btn btn-secondary mt-4 btn-sm" %><br />
<% end %>

<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <%= link_to "نسيت كلمة المرور؟", new_password_path(resource_name), class: "btn btn-secondary mt-4 btn-sm" %><br />
<% end %>
