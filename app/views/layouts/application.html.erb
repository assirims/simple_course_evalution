<!DOCTYPE html>
<html class="h-100" dir="<%= I18n.locale == :ar ? 'rtl' : 'ltr' %>" rtl" lang="<%= I18n.locale %>">
  <head>
    <%= render 'shared/head' %>
  </head>

  <body class="d-flex flex-column h-100" style="font-family: 'Almarai', sans-serif;">

    <main class="flex-shrink-0">
      <%= render 'shared/navbar' %>
      <%= render 'shared/notices' %>
      <div class="container">
        <div class="bc">
          <div class="row h-100 align-items-center justify-content-center text-center">
            <div class="col">
              <%= yield %>
            </div>
          </div>
        </div>
      </div>
    </main>

    <%= render 'shared/footer' %>
  </body>
</html>
