<div class="card">
  <div class="card-body py-5 px-md-5">
    <h1 class="text-center mb-4"><%= t '.title' %></h1>

    <div class="card">
      <div class="card-body">

        <%= render "form", review: @review %>

        <br>

        <div>
          <%= link_to (t '.show_button'), @review, class: "btn btn-success mb-4" %>
          <%= link_to (t '.return_button'), reviews_path, class: "btn btn-info mb-4" %>
        </div>
      </div>
    </div>
  </div>
</div>
